# 2026-07-06 Rename/ROB 与 Commit/CSR 集成交接

## 1. 当前版本

- 仓库：`F:\RISCV_CPU`
- 分支：`codex/full-rtl-refactor`
- 基线提交：`388daef Integrate rename allocation checkpoints`
- 目标器件：`xc7k325tffg900-2`
- 完整核目标：route 后 200 MHz，setup/hold 收敛且无未约束内部路径。

## 2. 本版本完成内容

- ROB 新增精确异常 `exception_flush/done`，可抢占 branch clear/restore scan。
- RAT/AMT 导出只读 AMT map，供 Free List 异常重建。
- `rename_rob_cluster` 闭合 Decode→Rename→资源分配→ROB 原子 fire、ROB entry 构造、
  branch tail restore 和四路 sticky recovery ack。
- 去除 ROB ready 反向依赖 alloc valid 的组合环。
- `rob_alloc_t` 增加 CSR/system command、prepared operand 与原始指令字段。
- `commit_csr_cluster` 实现合法 CSR、非法 CSR、MRET、ECALL/EBREAK、FENCE 的 ROB-head
  提交语义，并保留普通双提交与 Store 两阶段协议。
- CSR 固定由 Issue Arbiter 路由到 INT0；INT0 只捕获 rs1，ROB completion 保存并前递
  `csr_operand`，执行阶段不修改 CSR、不写 PRF。
- `commit_csr_prf_cluster` 闭合 CSR 提交写与 PRF ready 握手；普通 WB 活跃时 CSR 原子
  提交暂停，避免同 Bank 写回丢失。
- `commit_recovery_cluster` 集成 Rename/ROB、Commit/CSR/PRF 与 Recovery Controller，
  闭合 commit map/reclaim、PRF ready clear、四路恢复 ack 和最终 redirect。
- Commit reclaim 增加 valid/ready 反压，CSR 状态修改增加独立 commit 使能；恢复 busy
  期间抑制重复异常副作用。
- 新增 `backend_int_cluster` 作为首个后端整数集成边界，串接 Commit/Recovery、
  Dispatch Buffer、INT Issue Queue、Issue Arbiter、Operand Read、INT0、INT1/Branch
  和 Writeback Arbiter。当前阶段 LSU/MDU 分派保持反压，仅开放 INT/Branch/CSR loop。
- `backend_int_cluster` 在分支解析和 ROB completion 之间加入一项 resolve 缓冲：分支
  recovery 只在匹配 ROB completion 已经可见时进入 recovery controller，避免误预测恢复
  扫描抢占并丢弃分支自身完成位。
- INT0 CSR prepare 修正立即数型 CSR operand：`CSR_RWI/RSI/RCI` completion data 使用
  5-bit zimm 零扩展，寄存器型 CSR 继续使用 rs1；CSR prepare 不提前写 PRF。

## 3. 验证与 OOC

- QuestaSim 2024.1：全量编译/仿真 `Errors: 0`，保留默认端口 kind 警告；33 项
  directed regression 全部 PASS，新增 `tb_backend_int_cluster`。
- ROB exception flush：5 ns OOC WNS `+0.903 ns`。
- Rename + ROB Cluster：初次 WNS `-0.281 ns`、2 个组合环；去环后 WNS `+0.559 ns`、
  TNS `0`、combinational loops `0`。
- Commit + CSR Cluster：5 ns OOC WNS `+1.202 ns`。
- Commit + CSR + PRF Cluster：5 ns OOC WNS `+1.013 ns`，时序健康并冻结。
- Commit + Recovery Cluster：首次 5 ns OOC WNS `-2.104 ns`、TNS `-3198.714 ns`，
  loops `0`；最差路径 18 级、route 占 82.6%。已加入 ROB 一拍 head refill 切断反馈，
  首次复测 WNS `-1.516 ns`、TNS `-2089.456 ns`。新路径落在 payload `/R`，已取消
  失效 payload 清零、只清 valid/complete。第二次复测 WNS `-1.210 ns`、TNS
  `-4780.215 ns`，路径转移到 occupancy/minstret；已建立两阶段提交寄存边界并延迟
  minstret 增量。第三次复测 WNS `-1.015 ns`，最差路径转到 commit recovery
  同拍回灌 ROB tail reset，次差路径为 CSR writeback 同拍驱动 PRF ready。现已将 commit
  recovery 请求寄存后送入 recovery controller，并为 CSR commit write 加一项缓冲；
  `csr_wb_pending` 纳入外层 busy/hold。第四次复测 WNS `-0.599 ns`、TNS
  `-62.066 ns`、失败端点 203 个，最差路径转为 commit map 同拍更新 RAT/AMT CE。
  现已把 commit map、reclaim 和 retire_count 合并为已握手提交事务寄存器，下一拍
  统一推进 AMT/Free List/ROB。第五次复测 WNS `+0.121 ns`、TNS `0`，但
  `check_timing` 报告 2 个 combinational loops；不冻结。已修正 pending reclaim
  valid/ready：pending 事务稳定输出 reclaim valid，pending 清除和 AMT/ROB 推进才看
  reclaim ready，切断 `valid -> ready -> valid` 回环。Questa 32 项通过，等待复综合
  确认 loops 为 0。第六次复测 WNS `+0.121 ns`、TNS `0`、失败端点 `0`、
  loops `0`，最慢路径回到普通 dispatch/ROB alloc CE，当前冻结。
- Backend INT Cluster：首次 5 ns OOC WNS `-0.975 ns`、TNS `-41.669 ns`，
  失败端点 226 个；最差路径为 INT Issue Queue `rob_id_q` 经 issue arbiter grant
  回灌 IQ `pair_*` 寄存器，route 占约 84%。已切断 IQ S0 对当前 grant 的组合依赖：
  S0 只看上一拍 deferred clear，S1 过滤 pending clear，candidate 输出在 clear 窗口
  屏蔽已 grant group；arbiter P0 也不再重新采样同周期已 grant group。
- 第二次复测 WNS `-0.639 ns`、TNS `-126.538 ns`、失败端点 309 个，loops `0`。
  最差路径已转移为 IQ candidate payload 到 Issue Arbiter P0 `proposal_uop_q`，route
  占约 86%。已在 Issue Arbiter 前端加入 C0 candidate snapshot：P0 从快照候选生成
  proposal，P2 仍使用 IQ 实时 candidate ROB-ID 复核后才 grant，避免 stale proposal
  误发射。该修订增加一拍 issue arbitration latency，用寄存边界切断 IQ 到 Arbiter
  proposal 的跨模块长布线路径。
- 第三次复测 WNS `-0.990 ns`、TNS `-267.200 ns`、失败端点 312 个，loops `0`。
  最差路径显示 C0 后 P0 仍组合依赖当前 `issue_grant_o`，使 P2 realtime
  revalidate/grant 链重新回到 `proposal_uop_q`。现已删除 P0 对当前 INT/MEM/MDU
  grant 的跳过判断，C0/P0 完全只看快照候选；重复快照由 P2 的实时 ROB-ID/source
  valid 复核自然抑制。
- 第四次复测 WNS `-0.037 ns`、TNS `-0.037 ns`、失败端点 1 个，loops `0`。
  残余路径为 Issue Arbiter registered issue output 经 Operand Read ready 反馈到 P0
  proposal。现已将 P0 使用的 INT/LSU/MDU ready 也纳入 C0 snapshot；P2 仍用实时
  ready/allow 做最终 revalidate/grant。该修订用一拍保守 ready 视图切断
  `issue output -> operand_read ready -> P0 proposal` 反馈。
- 本次 timing cut 暴露了 ROB head 行 bank0/bank1 可分拍完成时的提交边界问题。ROB
  现支持 `head_bank_q` 半行 head：lane0 单独提交后，bank1 在下一拍作为 head lane0
  暴露，避免同一 ROB entry 重复 reclaim；`commit_recovery_cluster` 额外保持一拍
  ordinary reclaim drain busy，使 backend idle 等待 Free List reclaim FIFO 计数真正可见。
- Backend INT directed test 覆盖双整数 ALU 完整 loop、分支误预测
  recovery/redirect/checkpoint release，以及 CSR prepare/commit/PRF ready。当前 33 项
  Questa regression 全部 PASS。第五次 OOC 复测 WNS `+0.110 ns`、TNS `0`、
  失败端点 `0`、loops `0`；最慢路径已离开 issue feedback，转为 recovery controller
  latched recovery valid 到 commit PRF bank0 read-data register，route 占约 84.6%。
  该集群当前时序健康并冻结。

## 4. 下一步

Backend INT Cluster 已完成集成时序闭合并冻结：5.000 ns OOC WNS `+0.110 ns`、
TNS `0`、失败端点 `0`、loops `0`，Questa 33 项回归通过。下一步按计划进入后续
后端集成/LSU-MDU 相关边界；报告中仍有 1 个 input port 未设置 input delay，后续
顶层约束整理时一并补齐。
